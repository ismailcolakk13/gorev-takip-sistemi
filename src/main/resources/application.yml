spring:
	datasource:
		url: jdbc:${DB_URL:mysql://localhost:3306/taskdb}
		username: ${DB_USERNAME:ismail}
		password: ${DB_PASSWORD:1234}
	jpa:
		hibernate:
			ddl-auto: update
		properties:
			hibernate:
				dialect: org.hibernate.dialect.MySQLDialect
